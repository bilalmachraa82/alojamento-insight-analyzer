# Cloudflare Pages Environment Variables Configuration
# Copy this file to configure your Cloudflare Pages deployment

# ============================================================================
# IMPORTANT: How to Set Environment Variables in Cloudflare Pages
# ============================================================================
# 1. Go to your Cloudflare Dashboard
# 2. Navigate to Workers & Pages > Your Project > Settings > Environment Variables
# 3. Add each variable below as either:
#    - Production: Variables for production deployments (main branch)
#    - Preview: Variables for preview deployments (all other branches)
# 4. Click "Encrypt" for sensitive values (API keys, secrets)
# 5. Save and redeploy your project
# ============================================================================

# ============================================================================
# REQUIRED: Supabase Configuration
# ============================================================================
# Get these from your Supabase project settings:
# https://app.supabase.com/project/_/settings/api

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Alternative variable name (some projects use this)
VITE_SUPABASE_PUBLISHABLE_KEY=your_supabase_anon_key

# ============================================================================
# REQUIRED: Hugging Face API Configuration
# ============================================================================
# Used for sentiment analysis
# Get your API key at: https://huggingface.co/settings/tokens
# Create a token with "Read" permissions

VITE_HUGGINGFACE_API_KEY=your_huggingface_api_key

# ============================================================================
# REQUIRED: Apify API Configuration
# ============================================================================
# Used for web scraping (Booking.com reviews)
# Get your API key at: https://console.apify.com/account/integrations

VITE_APIFY_API_KEY=your_apify_api_key

# ============================================================================
# OPTIONAL: Email Service Configuration (Resend)
# ============================================================================
# Only needed if using email notifications
# Get your API key at: https://resend.com/api-keys

VITE_RESEND_API_KEY=your_resend_api_key

# ============================================================================
# OPTIONAL: Analytics Configuration
# ============================================================================
# Google Analytics 4
VITE_GA4_MEASUREMENT_ID=G-XXXXXXXXXX

# Cloudflare Web Analytics (automatically available on Cloudflare Pages)
# No configuration needed - enable in Cloudflare dashboard

# ============================================================================
# OPTIONAL: Monitoring & Error Tracking (Sentry)
# ============================================================================
# Sentry DSN for error tracking
# Get from: https://sentry.io/settings/projects/your-project/keys/

VITE_SENTRY_DSN=https://your-sentry-dsn@sentry.io/your-project-id
VITE_SENTRY_AUTH_TOKEN=your_sentry_auth_token
VITE_SENTRY_ORG=your-sentry-org
VITE_SENTRY_PROJECT=your-sentry-project

# ============================================================================
# ENVIRONMENT-SPECIFIC SETTINGS
# ============================================================================
# These should be set automatically by Cloudflare, but you can override them

# NODE_ENV is set by Cloudflare automatically:
# - "production" for production branch
# - "preview" for preview branches
NODE_ENV=production

# ============================================================================
# BUILD CONFIGURATION
# ============================================================================
# These are used during the build process in Cloudflare Pages

# Build command (configured in Cloudflare dashboard)
# Production: npm run build:production
# Preview: npm run build:preview

# Build output directory (configured in Cloudflare dashboard)
# dist

# Node version (configured in Cloudflare dashboard or in .nvmrc)
# 18.x or 20.x recommended

# ============================================================================
# CLOUDFLARE PAGES AUTOMATIC VARIABLES
# ============================================================================
# These are automatically provided by Cloudflare Pages:
# - CF_PAGES=1
# - CF_PAGES_COMMIT_SHA=<commit hash>
# - CF_PAGES_BRANCH=<branch name>
# - CF_PAGES_URL=<unique deployment URL>

# ============================================================================
# SECURITY NOTES
# ============================================================================
# 1. NEVER commit real API keys to your repository
# 2. Use "Encrypt" option in Cloudflare dashboard for sensitive values
# 3. All VITE_ prefixed variables are exposed to the client
# 4. For server-side secrets, use Cloudflare Workers/Functions
# 5. Rotate API keys regularly
# 6. Use different keys for production vs preview environments

# ============================================================================
# ENVIRONMENT VARIABLE PRIORITIES
# ============================================================================
# 1. Preview Environment: Variables set in Cloudflare for preview deployments
# 2. Production Environment: Variables set in Cloudflare for production
# 3. Both: Variables available to both environments (use sparingly)

# ============================================================================
# DEPLOYMENT CHECKLIST
# ============================================================================
# Before deploying, ensure you have:
# ✓ Set all REQUIRED environment variables in Cloudflare dashboard
# ✓ Encrypted all sensitive API keys
# ✓ Tested with preview deployment first
# ✓ Verified Supabase connection
# ✓ Tested API integrations (Hugging Face, Apify)
# ✓ Set up custom domain (if applicable)
# ✓ Configured DNS records (if using custom domain)
# ✓ Enabled Cloudflare Web Analytics
# ✓ Set up error monitoring (Sentry)
# ✓ Verified all environment-specific settings
